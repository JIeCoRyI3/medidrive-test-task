import { createTheme } from '@mui/material/styles'

export const theme = createTheme({
  palette: {
    mode: 'light',
    primary: {
      main: '#0a6a88',
    },
    secondary: {
      main: '#f57c00',
    },
  },
  shape: {
    borderRadius: 10,
  },
})
